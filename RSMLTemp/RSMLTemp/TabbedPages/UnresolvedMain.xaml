<?xml version="1.0" encoding="utf-8" ?>
<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RSMLTemp.TabbedPages.UnresolvedMain"
            xmlns:pages="clr-namespace:RSMLTemp.TabbedPages">
    <pages:ResolvedMain/>
    <ContentPage Title="UNRESOLVED">
        <StackLayout>
            <ListView x:Name="UnresolvedList" ItemSelected="UnresolvedList_ItemSelected" SeparatorColor="Black">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <ViewCell.View>
                                <StackLayout BackgroundColor="LightGray">
                                    <Label Text="{Binding TimeOccured}" TextColor="DarkRed"/>
                                    <Label Text="{Binding Department}" TextColor="DarkRed"/>
                                </StackLayout>
                            </ViewCell.View>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage>
</TabbedPage>